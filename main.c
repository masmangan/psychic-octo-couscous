#include <stddef.h>
#include <stdlib.h>
#include <stdio.h>

char *decode(const unsigned char *code, size_t n, unsigned key)
{
    // itoa on key
    // keys counts from k - ´0´
    // codes decoded as get(c - k)
    // codes in an array, starting  at ´a´ on a[0]
    return NULL;
}

int main()
{
    printf("TESTE\n");
    return 0;
}